name: Testing the reusbale workflows

on:
  push:
    branches: "**"
jobs:
  test_workflows:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Set up workspace
        uses: jeeshan12/resusable_workflows/.github/actions/setup/workspace@v2.0.4
        with:
          language: "node"
          version: "14.x"
